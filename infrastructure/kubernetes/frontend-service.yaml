apiVersion: v1
kind: Service
metadata:
  name: capx-frontend
  namespace: capx-platform
  labels:
    app: capx-frontend
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: capx-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  sessionAffinity: None
